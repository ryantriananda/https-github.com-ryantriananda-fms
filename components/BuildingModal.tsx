
// This component is deprecated and has been replaced by BuildingFormPage.tsx
// All building form logic is now handled in the full-page component for better UX.

import React from 'react';

interface Props {
  isOpen: boolean;
  onClose: () => void;
  // Other props are irrelevant now
}

export const BuildingModal: React.FC<any> = () => {
  return null;
};
